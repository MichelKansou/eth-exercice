import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    row: {
        marginBottom: 15
    },
    text: {
        color: 'gray',
        fontSize: 20
    }
});

export default styles;
